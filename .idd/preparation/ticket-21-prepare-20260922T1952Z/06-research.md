# RESEARCH report

Execution identity: fresh sub-agent `itdd21-research`, RLM child id `sub-842dddeb`; session artifacts `/Users/herbertfields/.prime/agent/session-artifacts/01a0c9a1-c6cf-7659-a446-ada53a2f1aee/sub-842dddeb`. Read-only; no tests/edits.

Approved source/baseline: issue #21 and parent #15; baseline SHA `80284553d571d5d92e2a8f66b7b8c312f9c283ef`.

1. Proposed source/test paths: historic baseline-to-candidate diffs of both failed cycles identify `control/integration.py` and `tests/integration/test_stage_e7_integration.py`. This aligns with #21 integration/promotion scope. These are proposed allowlist paths; they are pre-existing source/test files at baseline. Exclude all other paths including unrelated lifecycle/maintenance, runtime/manifest, schema/evidence, issue artifacts and test-generated locks/cache unless a scope-change decision is made.
2. At baseline, `pyproject.toml` configures pytest (`testpaths=tests`, `test_*.py`, `-v --tb=short`), dependencies include pytest and optional dev pytest-cov/black/ruff. No type checker/authoritative typecheck command found. `pytest.ini` exists, and must be inspected in contract. Historic `uv run pytest -q` had 16 collection import errors. A `sys.modules['tests']` shim made 4 target / 214 full tests pass but is disallowed and cannot count. VERIFY archive `plain-pytest.log` shows same issue; shim-based 217 pass cannot count. No CI config or proven normal no-shim command found in checked sources. Status: unresolved test-entrypoint blocker; research follow-up required.
3. Workflow: `skills/idd/SKILL.md` makes `/idd` the top-level authority/router; it routes approved exact contract to `/itdd-execute`, which launches fresh BUILD/TEST/VERIFY. `skills/itdd-execute/SKILL.md` requires exact contract, isolated `.idd/build_workspaces/`, fail-fast, immutable failures. `/implement` is generic and says `/tdd`, checks, `/code-review`, commit current branch; use only within authorized BUILD role, subordinate to allowlist and no promotion/unauthorized commit. `skills/manifest.json` marks these as controller-routed; controller owns routing/evidence/lifecycle/promotion. Issue #21 `/implement` wording should be reconciled as a BUILD skill, not a separate authority path.
4. Historical tests: (a) all rejected operations need before/after byte/hash snapshots of canonical materialized state and exact event bytes, assert no mutation plus expected rejection class/reason. (b) create disposable overlapping Git branches; execute actual merge; confirm conflict event, abort, no result commit/tree, canonical HEAD/tree unchanged. (c) independent verifier must attest candidate commit/tree and reject tampering. (d) fresh-process reconstruction of event state. (e) verify durable conflict event/materialized state after restart. (f) malformed/unknown/recognized event schemas, discriminators, order, timezone chronology; fail closed and nonmutation. (g) verification-before-approval-before-promotion chronology; reject premature/duplicate/terminal transitions; operational events cannot masquerade as integration. (h) hostile admission/approval rejected at intended authorization boundary with exact nonmutation. (i) only expected durable files change for transitions. (j) fresh independent BUILD/TEST/VERIFY identity/provenance, prompts/output hashes. (k) Standards and Spec reviewers independently bound to exact diff and provenance. (l) continuous resource telemetry timestamps/limits/raw data. (m) changed paths and baseline/candidate hashes in role evidence. (n) no promotion before independent VERIFY passes. These are reported historical gaps; preserve old reports unchanged.
5. Parent #15 requires immutable evidence/history, fresh identities, append-only event/reducer boundary, fail-closed recovery, independent verification, clean disposable environment, no retroactive promotion. #21 requires integration verification, Git-attested results, conflict/failure routing, durable evidence, restart reconstruction. Relevant baseline sources: `docs/architecture/git-attested-integration.md`, `docs/architecture/verification-and-evidence.md`, `docs/architecture/isolation-and-events.md`, `docs/architecture/operational-lifecycle.md`, ADR-0002, ADR-0003, ADR-0005, `control/integration.py`, `tests/integration/test_stage_e7_integration.py`, event/git/verification/human-decision support files and integration/event/verification schemas.

Open facts: (1) exact two-file allowlist is strongly supported but must be explicit in the contract; (2) native test command with no shim is not established; (3) no typecheck command found; (4) role/command details must match actual `/idd` route.
