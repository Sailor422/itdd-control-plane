ITDD #21 SPEC/TICKET DECOMPOSITION (read-only; exact execution contract proposal)

Authority/source: GitHub Sailor422/itdd-control-plane #21 “Complete clean integration and promotion” plus parent #15. Preserve immutable issue snapshots and SHA256s from `.idd/preparation/ticket-21-prepare-20260922T1952Z/04-source-snapshot-manifest.md` (#15 `25e82da24d666b0334d50af8bb8bb6e3827f1e79a416187a4e8856e1b5721d83`; #21 `c2ec49487b8af9763fba6c971d3f75208083da143cd41cafe7677dcd889b5cd6`). Selected baseline is exactly `80284553d571d5d92e2a8f66b7b8c312f9c283ef`, fresh isolated worktree only. Earlier `bc2b53b...` and candidates `330e106...`, `b1a818...` are forensic inputs only, never execution inputs. Human approved defaults, not this final contract: explicit approval of this exact contract remains prerequisite.

Bounded milestone: complete only the existing integration/promotion path: fresh integration verification, Git-attested results, real conflict/failure routing, durable evidence and reconstruction; promotion must be controller-owned and impossible before a controller-owned independent VERIFY PASS. Do not expand to generic lifecycle/recovery/maintenance, runtime/manifest, unrelated tickets, docs/config, or broad refactors. All historical reports/comments remain immutable, failures remain failures, new evidence is append-only.

Exact implementation/test path allowlist (only paths BUILD may change): `control/integration.py`; `tests/integration/test_stage_e7_integration.py`. Every other tracked path is excluded, including schemas, event/authorization/operational modules, tests outside this file, config/docs/skills, and all historical evidence. Also exclude dirty checkout paths, existing worktrees, generated/cache/lock files and untracked content. If required behavior cannot be implemented/tested within these two paths, stop and report a scope blocker; do not broaden. New role evidence must go only to a newly named append-only evidence directory if the approved controller protocol authorizes it; never overwrite prior artifacts. This is evidence output, not BUILD source allowlist.

Dependencies/entry: `/idd` is the public entry and routes the approved exact contract to `/itdd-execute`; it launches distinct fresh BUILD, TEST and VERIFY roles. `/implement` and `/tdd` are BUILD-only helpers, subordinate to this contract, not alternate authority/promotion routes. Require separately authorized Standards and Spec reviews bound to the exact candidate diff. Existing issue blockers #18/#19 are closed but do not establish #21 completion.

Behavior/dependencies to implement and prove: integration requests are authorized, validated and fail closed; verification is fresh and bound to immutable candidate commit/tree; result is Git-attested (commit and tree); concurrent overlapping Git changes invoke actual merge, route conflict durably, abort cleanly and never produce result commit/tree or alter canonical HEAD/tree; event log is append-only and projections reconstruct in a fresh process; approval/promotion enforce exact chronology and only follow controller-owned VERIFY PASS. Rejected admission, approval, and other rejection probes must assert byte-identical materialized state AND exact event bytes before/after, with expected rejection class/reason. Do not let operational events masquerade as integration events. Only intended durable files may change on accepted transitions.

Historical-gap → required probe/evidence matrix (every item is blocking):
* TEST #21 comment + `evidence/ticket-21-test-20260922T1917Z/TEST.md` (SHA `330c03f...`): (1) rejection durability gap → snapshot/hash canonical materialized state and exact event bytes pre/post each rejected admission/approval/transition; prove byte-identical, expected rejection and reason. (2) absent mechanical conflict probe → disposable branches with overlapping edits; run actual Git merge through integration route; demonstrate conflict detection/event, abort, no result commit/tree, canonical HEAD and tree unchanged. Semantic REPLAN_REQUIRED/interruption is not a substitute. (3) normal pytest import failures → documented setup and plain native command below must pass without `sys.modules` or other import shim; otherwise BLOCK, do not conceal/broaden.
* VERIFY full-scope report and #21 comment (candidate `b1a818...`, tree `35307...`, baseline `bc2b...`, report `/tmp/ticket-21-verify-full-scope/VERIFY.md`; historical only): (4) fresh independent verifier records and mechanically attests exact candidate commit and tree; detect tamper/mismatch. (5) fresh-process reconstruction from durable authority, including conflict event/materialized state after restart; no conversation/process-memory dependence. (6) conflict/rejection assertions also prove no result commit/tree and durable-state nonmutation. (7) strict recognized/unknown/malformed event schemas and discriminators; ordering and timezone-aware chronology; fail closed without mutation. (8) enforce verification-before-approval-before-promotion chronology, reject premature, duplicate and terminal transitions; prove no promotion before controller-owned VERIFY PASS. (9) operational events cannot satisfy integration/promotion predicates. (10) distinct fresh BUILD/TEST/VERIFY identities and provenance, prompts/output hashes, baseline/candidate hashes and changed-path manifests; no self-certification. (11) independent Standards and Spec reviews, each linked to exact candidate diff hash and reviewer identity/provenance. (12) continuous timestamped resource telemetry with limits and raw evidence. (13) clean disposable environment and only allowed path changes; preserve commands, runtime, raw outputs and hashes. The historic VERIFY’s 217 archive tests do not discharge any of these criteria.
* #21/#15 source acceptance: positive and hostile behavior at highest existing seam; accepted historical evidence preserved; evidence/history immutable and append-only; verify conflict/failure routing, Git attestation, fresh reconstruction; independent standards/spec review before completion. Do not claim whole #15 recoverable system is complete.

Acceptance: all above probes independently witnessed, exact allowlist clean, all expected state and event bytes verified, valid chronology, fresh-process reconstruction, clean no-shim test suite passes, independent reviews pass against same exact diff, telemetry/evidence complete, and independent controller-owned VERIFY PASS. Only then may the controller consider promotion. Any missing evidence, failed probe, path escape, provenance ambiguity, test collection/import error or resource limit breach is fail-fast: preserve evidence as FAIL/INCOMPLETE, do not retry by relaxing scope, do not promote/close.

Tests/operations: fresh isolated worktree at exact SHA; setup using baseline README documented editable install (`pip install -e .`), then plain `pytest -q`, no shim; run targeted integration tests as relevant, but full native suite is mandatory. No typechecker command is documented; do not invent one. No import shim. Record exact commands, environment/runtime, exit status and raw logs; baseline/candidate commit+tree, allowlist diff, source/issue hashes, event/state before-after bytes and hashes, merge branches/merge output/abort proof/HEAD-tree, reconstruction outputs, schema/chronology results, identities/model/role provenance, review-to-diff hashes, resource telemetry timestamps/limits/raw data, artifact hashes and final independent verdict. No commit, promotion, issue lifecycle change or cleanup until separate approved `/itdd-execute` controller flow.

Support/blocker status: paths and baseline are supported by prep research; behavior is proposed within integration seam. The documented native test command is clear but prior plain pytest had collection errors on other environment/setup (`uv run pytest`); this contract requires documented editable install + plain `pytest -q`, without shim. Whether that passes at exact baseline is not established, and must be verified in the authorized TEST role. If it fails within allowlist, report blocker; do not alter excluded import/package config. No typecheck exists. Exact two-file scope may prove insufficient for all historical gaps; this is a deliberate stop condition, not authority to expand.
