# Resumed RESEARCH — reviewer blocker analysis

Execution identity: fresh sub-agent `itdd21-review-blocker-research`, RLM child id `sub-63eb1a1e`; session artifacts `/Users/herbertfields/.prime/agent/session-artifacts/01a0c9a1-c6cf-7659-a446-ada53a2f1aee/sub-63eb1a1e`. Read-only; no tests/edits/worktrees/state change.

Controller route: BLOCKER. `skills/idd/SKILL.md` requires exact approved contract → `/itdd-execute`, 3 fresh executions and `.idd/build_workspaces/`; `skills/itdd-execute/SKILL.md` is procedural, not an executor/assignment API. `CONTEXT.md` requires controller-created assignment before launch and controller-preserved raw evidence. `OperationalController` methods `propose_plan`, `spawn_eu_operation`, `run_eu`, `_verify_axes`, `promote` in `control/operational.py` provide single-EU builder lifecycle, not distinct BUILD/TEST/VERIFY assignments/workspaces/evidence capture/custody. `tools/itdd_execute.py --accept` is narrow unrelated harness. Manual parent RLM spawn/copy not established as controller execution. No general supported `/idd`→`/itdd-execute` executor found.

Integration code/test fact findings: `IntegrationController.create` validates candidate count, attestation, project, separate SPEC+STANDARDS verification, optional human approval, common baseline, then creates worktree/capability/context and event. `run` actual Git merge catches `GitError`, aborts, emits CONFLICT/MECHANICAL_CONFLICT; semantic conflict/interruption are distinct statuses and not substitutes. `create_integration_verifier` requires completed/result commit; `mark_verified` requires verifier PASS and exact result commit; `evaluate_promotion_eligibility` requires INTEGRATION_VERIFIED and no WAITING human gate; `promote_verified` requires eligible, canonical HEAD=baseline, clean source tree, then ff-only merge and `promotion.completed`.

Existing `tests/integration/test_stage_e7_integration.py` fixtures/tests: `prepare` creates baseline and two candidate commits plus independent reviews. Positive E7 case creates/runs IN-701, checks ALLOW/DENY workspace capability, creates/launches verifier, marks verified, and approval gate. `test_git_identity_conflict_and_missing_human_approval` exercises invalid `attest(AT-802)` and rejected `create(IN-802, require_human=True)` with expected `GitError` and `IntegrationError("candidate lacks required human approval")`; assert no attestation/integration file and event bytes unchanged, but exact attestation storage path must be confirmed in `control/git.py`. `test_semantic_and_interrupted...` tests semantic REPLAN_REQUIRED and INTERRUPTED events only, not real merge. `test_verified_evidence_cannot_be_reused...` rejects `create(IN-951)` with `candidate is not independently Spec and Standards verified`; assert state/event nonmutation. Existing tests do not cover actual Git conflict/abort/no result commit/tree/canonical invariant; malformed/unknown events, schema, chronology/timezone, duplicate/terminal transitions; evidence tamper; promotion-before-verify/counterfeit operational events/canonical drift; restart reconstruction; exact pre/post snapshots. Multiple store paths live in modules outside the two-path edit allowlist; may be read but not edited.

Both blockers remain: no supported controller execution route, and full hostile matrix still requires case-by-case operation/input/boundary/reason/authority-file assertions. No tests run; no code/state changed. Historical review FAIL remains unchanged.
