# ITDD Execution Contract (prepared, not approved)

- **Preparation ID:** `ticket-21-prepare-20260922T1952Z`
- **Spec/ticket/intent identity and version:** GitHub `Sailor422/itdd-control-plane#21`, “Complete clean integration and promotion,” with parent `#15`, “Spec: complete the ITDD control plane as a recoverable system.” Immutable snapshots in `.idd/preparation/ticket-21-prepare-20260922T1952Z/issue-21-snapshot.json` (SHA-256 `c2ec49487b8af9763fba6c971d3f75208083da143cd41cafe7677dcd889b5cd6`) and `issue-15-snapshot.json` (SHA-256 `25e82da24d666b0334d50af8bb8bb6e3827f1e79a416187a4e8856e1b5721d83`); issue URLs are github.com/Sailor422/itdd-control-plane/issues/21 and `/15`. Source identity is the snapshots plus those issue identities, not a mutable branch tip.
- **Immutable source location(s):** Those snapshots and source manifest `04-source-snapshot-manifest.md`; preserve issue bodies/comments and historical evidence as-is. Historical TEST candidate `330e106bcb13eeeb6d7efa42cf941d37f6ee30ec` and VERIFY candidate `b1a8184af79bdc3376e0c8b5c9b4c5ae47f0d91a` (tree `35307ed34c613d55497e6fae6a40267e9efb15c3`) and prior baseline `bc2b53b6b989cac8ad9ad5948385fe2a65799669` are forensic history only, not execution inputs.
- **Baseline SHA (full):** `80284553d571d5d92e2a8f66b7b8c312f9c283ef` (`main` at preparation decision); use only in a fresh, controller-created isolated workspace. Never use dirty ordinary checkout or existing/old worktrees/candidates.
- **Allowed paths:** BUILD may change exactly `control/integration.py` and `tests/integration/test_stage_e7_integration.py`. No other candidate paths. If behavior or test criteria require another path, fail/stop with scope blocker; do not expand allowlist. Fresh isolated controller-created workspace only.
- **Allowed operations:** After explicit approval of this exact contract only, `/idd` routes to `/itdd-execute`; exactly separate fresh BUILD, TEST, VERIFY in order. `/implement` and `/tdd` are BUILD-only helpers inside authorized BUILD. TEST/VERIFY inspect and probe candidate independently. Run documented setup `pip install -e .`, then plain full-suite `pytest -q`; targeted E7 tests may supplement, never replace full suite. No import shim. No typecheck (none configured). Controller owns commit, candidate registration, evidence routing/lifecycle and any later promotion. Stop at first material failure and preserve it; no retries inside same acceptance cycle.
- **Acceptance criteria:**
  1. Implement only the #21 integration/promotion path within two-file allowlist; preserve all historical reports/failures and accepted evidence unchanged.
  2. Fresh integration verification is bound to immutable candidate commit/tree; independent VERIFY mechanically attests exact candidate identity and detects tampering/mismatch. Results are Git-attested (commit and tree).
  3. Real disposable overlapping Git branches exercise actual merge through integration route. Prove conflict detection/routing, durable conflict event/materialized state, clean abort, no result commit/tree, and canonical HEAD/tree unchanged.
  4. Durable event history is append-only; fresh-process reconstruction from durable authority works, including conflict state after restart and without conversation/process-memory dependence. Only intended durable files change on accepted transitions.
  5. For every rejected admission, approval and other rejection probe: execute operation at intended authorization boundary; capture canonical materialized-state and exact event bytes before/after; prove byte/hash identity and expected rejection class/reason. No unauthorized artifact accepted.
  6. Strict recognized, unknown and malformed event schema/discriminator handling; ordering and timezone-aware chronology; fail closed without mutation. Enforce verification-before-approval-before-promotion; reject premature, duplicate and terminal transitions. No promotion before controller-owned VERIFY PASS. Operational events cannot satisfy integration/promotion predicates.
  7. Fresh distinct BUILD/TEST/VERIFY identities and provenance; record prompt/output hashes, runtime/model/role, source/baseline/candidate hashes, changed paths and commands. No self-certification.
  8. Independent Standards and Spec reviews each bind reviewer identity/provenance to exact candidate diff hash and pass.
  9. Continuous timestamped resource telemetry with limits and raw evidence; clean disposable environment; complete commands, environment/runtime, raw outputs, exit statuses and hashes.
  10. Full documented no-shim `pytest -q` passes after editable install. Any collection/import failure is a blocking failure; do not conceal or fix outside allowlist. All criteria independently witnessed; controller-owned independent VERIFY PASS required before controller may consider promotion.
- **Positive tests** (command, expected result):
  1. From exact baseline in fresh workspace: `pip install -e .` succeeds; `pytest -q` runs full suite and exits 0. No shim. Failure blocks.
  2. Targeted E7 integration tests in `tests/integration/test_stage_e7_integration.py`; expected applicable positive integration/verification/Git-attestation/reconstruction tests pass.
  3. Fresh-process reconstruct durable accepted integration and conflict state; expected exact state/event-derived projections and chronology.
- **Negative/hostile tests** (operation, intended boundary, rejection, state assertion):
  1. Attempt rejected admission, unauthorized/premature/duplicate approval and invalid transition at each specified authorization/chronology boundary. Expect exact rejection class/reason there; raw materialized state and event bytes/hashes byte-identical before/after, no unauthorized artifact.
  2. Malformed/unknown/recognized-invalid event and discriminator/order/timezone cases; expect fail-closed schema/order/chronology result at parser/reducer boundary and no state/event mutation.
  3. Actual overlapping-branch Git merge via integration path; expect real merge conflict, durable conflict routing/event, then abort. Prove no result commit/tree and canonical HEAD/tree unchanged before/after. Semantic REPLAN_REQUIRED/interruption alone is not a pass.
  4. Tamper candidate commit/tree or evidence binding in verifier probe; expect independent verifier detects mismatch and refuses PASS/promotion.
  5. Attempt promotion before controller-owned VERIFY PASS, using operational event as counterfeit integration evidence, and after terminal/duplicate transition; expect exact rejection at promotion predicate and unchanged authority bytes/state.
- **Artifacts and evidence** (path, owner, hash/provenance):
  1. Source snapshots/hashes and all preparation reports under `.idd/preparation/ticket-21-prepare-20260922T1952Z/`; preserve append-only. Existing TEST and VERIFY failures remain immutable.
  2. Proposed unique run evidence root: `.idd/evidence/ticket-21-execute-20260922T1952Z/` (one run-specific, append-only directory; no overwrite). Controller must establish/authorize this exact route before execution. Repo handoff documents `.idd/build_workspaces/` for workspaces and durable evidence generally, but no supported exact evidence-storage API/path is established in inspected docs. This is a concrete unresolved controller-path blocker, not authorization to write there.
  3. In that root, controller-owned per-role immutable records: execution ID, role, agent/runtime/model identity, prompt/context and output hashes, timestamps, commands/operations, raw stdout/stderr and exit/status, environment, baseline/candidate commit and tree hashes, changed-path manifest, artifact hashes; append-only result. Controller owns commit/candidate and lifecycle artifacts.
  4. TEST/VERIFY raw hostile-probe evidence: exact requested operation, intended/actual enforcement boundary, reason/class; raw pre/post bytes plus hashes for canonical state and exact event log; merge branch refs/commands/raw output/abort proof and canonical HEAD/tree before-after; reconstruction output; schema/order/chronology results; no result commit/tree proof.
  5. Independent review reports and reviewer provenance, each naming exact diff hash; timestamped continuous resource telemetry, limits and raw samples; full install/test logs and outputs. Hash every artifact and link to run and source identities.
  6. If two-file allowlist cannot support any criterion or documented no-shim full suite fails, preserve FAIL/INCOMPLETE evidence and stop. No package/import fixes outside allowlist.
- **Explicit exclusions:**
  1. Dirty ordinary-checkout paths and all existing worktrees, including `/private/tmp/ticket-21-build`, `/private/tmp/ticket-21-corrective-build`, `/private/tmp/ticket-21-provenance-verify/worktree`; old candidates and historical baselines are not execution inputs.
  2. Any candidate path beyond the two-file allowlist; schemas, event/authorization/operational modules, other tests, docs/config/skills, generated/cache/lock files. No allowlist expansion.
  3. Any modification, replacement, cleanup, or retroactive promotion of historical evidence; all historical failures remain failures.
  4. Import shims, package/import fixes outside allowlist, invented typecheck, generic lifecycle/recovery/maintenance, unrelated tickets, issue closure, and promotion absent controller-approved VERIFY PASS.
  5. No commit, worktree creation, lifecycle change, promotion, or cleanup by this drafting phase; execution only via approved controller flow.
- **Preparation phase evidence directory:** `.idd/preparation/ticket-21-prepare-20260922T1952Z/`
- **Fresh execution IDs (discovery, grilling, research, decomposition, drafting, review):** Discovery `sub-c810b74a`; grilling `sub-e38ec296`; research `sub-842dddeb`; research closure `sub-2e1b312a`; decomposition `sub-37daf49e`; drafting `sub-b6da7073` (session artifact directory `/Users/herbertfields/.prime/agent/session-artifacts/01a0c9a1-c6cf-7659-a446-ada53a2f1aee/sub-b6da7073`); independent review pending.
- **Independent review:** PENDING; fresh independent reviewer must review exact contract and evidence, with identity and evidence path recorded. No PASS claimed.
- **Human approval:** PENDING until explicit approval of exact contract; preparation defaults approval is not exact-contract approval. Record approver, timestamp and contract hash only after that decision.

**Unresolved blockers before handoff can be considered complete:** controller must establish/authorize exact evidence path/storage route; independent review must run and record identity/evidence. The native tests and two-file sufficiency are intentionally execution-time fail-fast criteria; neither is claimed proven. Contract remains pending human approval; this draft does not authorize execution.
