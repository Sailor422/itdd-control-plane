# Supplemental RESEARCH: execution evidence routing

Execution identity: fresh sub-agent `itdd21-evidence-routing-research`, RLM child id `sub-d4ef6982`; session artifacts `/Users/herbertfields/.prime/agent/session-artifacts/01a0c9a1-c6cf-7659-a446-ada53a2f1aee/sub-d4ef6982`. Read-only; no tests or execution.

At baseline `80284553d571d5d92e2a8f66b7b8c312f9c283ef`, the generic `/idd` → `/itdd-execute` documentation specifies roles/evidence fields but no generic execution implementation or role-output directory/copy/attestation path. `tools/itdd_execute.py` implements only a narrow CODEX-RUNTIME-ROLES-V1 `--accept` harness, not this general ticket. It generates `work/proofs/itdd-runtime-roles-acceptance-<UTC-second>/`, scratch files and execution ID/process data; this harness is out of scope for #21 and can collide by second.

Generic `itdd-execute` prose requests actual session/process identity where available, but does not specify a path or capture API. RLM runtime in this session exposes each fresh child ID, model, session artifacts directory and explicit messages; OS process/PID or codex conversation ID is not provided by that generic skill. Existing project evidence convention includes `evidence/` and `work/proofs/` but no generic run-root standard. The old `evidence/accepted` and `prime-single-eu` artifacts are examples from a different adapter/harness, not proof of generic routing.

Decision needed in contract: explicitly define a unique run-specific append-only evidence root managed by the parent execution orchestrator; each RLM role writes raw artifacts in its own isolated session directory and candidate workspace; parent captures/copies artifacts plus hashes and records RLM child ID, model, session directory, prompt/output hashes, timestamps (mark PID unavailable). Do not claim `tools/itdd_execute.py` is the executor. Could use `evidence/ticket-21-execution-<unique-attempt-id>/` following existing ticket-evidence convention and `.idd/build_workspaces/<attempt-id>/<role>/` per `/idd` workspace contract. All evidence routing/controller actions require exact-contract approval.
