interface:
  display_name: "IDD Router"
  short_description: "Route work through preparation or approved ITDD execution"
  default_prompt: "Use /idd to route unapproved work to preparation and approved contracts to execution."
policy:
  allow_implicit_invocation: false
